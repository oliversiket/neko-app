import React, { Fragment } from 'react';
import Header from '../Header/Header';
import Content from '../Search/Search';

const App = () => (
	<Fragment>
		<Header/>
		<Content/>
	</Fragment>
);

export default App;
